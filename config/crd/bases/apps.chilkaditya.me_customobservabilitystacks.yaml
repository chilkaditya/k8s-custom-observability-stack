---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: customobservabilitystacks.apps.chilkaditya.me
spec:
  group: apps.chilkaditya.me
  names:
    kind: CustomObservabilityStack
    listKind: CustomObservabilityStackList
    plural: customobservabilitystacks
    singular: customobservabilitystack
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: CustomObservabilityStack is the Schema for the customobservabilitystacks
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CustomObservabilityStackSpec defines the desired state of
              CustomObservabilityStack.
            properties:
              cpuThreshold:
                format: int32
                type: integer
              enableDashboard:
                type: boolean
              memoryThreshold:
                format: int32
                type: integer
              targetDeployment:
                description: |-
                  Foo is an example field of CustomObservabilityStack. Edit customobservabilitystack_types.go to remove/update
                  Foo string `json:"foo,omitempty"`
                type: string
              targetNamespace:
                type: string
            required:
            - cpuThreshold
            - enableDashboard
            - memoryThreshold
            - targetDeployment
            - targetNamespace
            type: object
          status:
            description: CustomObservabilityStackStatus defines the observed state
              of CustomObservabilityStack.
            properties:
              lastReconcileTime:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
